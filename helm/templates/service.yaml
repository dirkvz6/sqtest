apiVersion: v1
kind: Service
metadata:
  name: {{ include "sanquote-dev.fullname" . }}
  labels:
    {{- include "sanquote-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sanquote-dev.selectorLabels" . | nindent 4 }} 
